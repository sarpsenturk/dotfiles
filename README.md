# dotfiles

A set of dotfiles I use commonly, from system configuration to
project/compiler configuration.

## Structure

All dotfiles associated with a tool, programming language, or environment
lives in its own subdirectory within the repo.